<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Connexion à l'administration</title>
    {{ encore_entry_link_tags('css/app') }}
    <style>
        body{
            display: flex;
            justify-content: center;
            height: 100vh;
        }
        .card{
            margin-top: 10%;
        }
    </style>
</head>
<body>
    <div class="col-md-4">
        <div class="card bg-light">
            <div class="card-header">
                <h4 class="card-title"><i class="fas fa-lock mr-2"></i>Connexion à l'administration</h4>
            </div>
            <div class="card-body">
                {% if hasError %}
                    <div class="alert alert-warning">
                        <p class="mb-0">L'adresse email ou le mot de passe est incorrect</p>
                    </div>
                {% endif %}
                
                <form method="post">
                    <div class="form-group"><label for="_username"></label>Email<input type="text" class="form-control" placeholder="Adresse email" name="_username" value="{{username}}"></div>
                    <div class="form-group"><label for="_password"></label>Mot de passe<input type="password" class="form-control" placeholder="Mot de passe" name="_password"></div>
                    <div class="form-group">
                        <button type="submit" class="btn btn-primary"><i class="fas fa-lock-open mr-2"></i>Connexion</button>
                        <a href="{{path('homepage')}}" class="btn btn-link"><i class="fas fa-arrow-circle-left mr-2"></i>Retour au site</a>
                    </div>
                </form>
            </div>
        </div>
    </div> 
    <script src="https://kit.fontawesome.com/79292a4c58.js"></script>
</body>
</html>